name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  rust:
    name: Rust ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Format
        run: cargo fmt --check

      - name: Test
        run: cargo test

      - name: Check CLI binaries
        run: cargo check -p mge-cli --bins

      - name: Build product release binaries
        run: cargo build -p mge-cli --bin mge --bin mge-mcp-server --release

      - name: Product release smoke
        if: runner.os == 'Linux'
        run: bash scripts/smoke-release.sh

      - name: Product release smoke
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./scripts/smoke-release.ps1
